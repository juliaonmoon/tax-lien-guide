# Handoff

## ⚡ In-flight work

**Clean stop.** No collector or fix is mid-implementation. The last thing
done was investigating a Hillsborough County, FL tax-deed collector — API
fully mapped, then deliberately stopped at a real correctness question
(which `Case Status` value means "available") rather than guess. That's
now written up as a pending item in `STATUS.md` — nothing further to do
here except pick a next task.

Last action: merged PR #11 (`docs: map Hillsborough tax-deed API in full,
flag status-mapping blocker`) to `main`. Local `main` is up to date with
`origin/main`.

Next concrete step (pick one, or something else — nothing is committed to
a direction): resolve the Hillsborough status-mapping question with real
evidence, check whether Hamilton County IN's list has been published yet,
or start on a different state entirely.

## ❓ Open decisions

None pending. The user said "keep going" / "you decide" for backlog
prioritization generally — no specific open question is blocking work.

## 🆕 New gotchas this session

None outstanding — everything discovered this session is already folded
into `STATUS.md` (this is that file's first version).

## 📁 Project path

`C:\Users\jules\tax-lien-guide` — a full local git clone (not GitHub-only).
No project-specific Claude Code transcript directory exists yet under
`~/.claude/projects/`; this session's own working directory was
`C:\Users\jules`, not this repo, so its transcript lives in the generic
`C--Users-jules` project folder (see below).

## 📜 Transcript path

`C:\Users\jules\.claude\projects\C--Users-jules\0f7edcd4-ddc2-4ae0-911e-63a53e00c8fd.jsonl`
— grep only on demand, do not read eagerly (this is a long, wide-ranging
session covering multiple projects, not just tax-lien-guide).
