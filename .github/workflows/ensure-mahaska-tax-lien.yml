name: Ensure Mahaska Iowa tax lien market

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ensure-mahaska-tax-lien.yml"
      - "scripts/add_iowa_mahaska_tax_lien_market.py"
      - "scripts/validate_iowa_mahaska_market.py"

permissions:
  contents: write

concurrency:
  group: ensure-mahaska-iowa-tax-lien-market
  cancel-in-progress: false

jobs:
  ensure-mahaska:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Publish Mahaska market from latest main
        run: |
          git config user.name "tax-lien-data-bot"
          git config user.email "actions@users.noreply.github.com"

          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main

            python scripts/add_iowa_mahaska_tax_lien_market.py
            python scripts/validate_iowa_mahaska_market.py
            python scripts/add_max_return_rates.py
            python scripts/add_tax_lien_county_filter.py
            python scripts/recount_tax_lien_properties.py

            if git diff --quiet -- data/ index.html; then
              echo "Latest main already contains the generated Mahaska County market output."
              exit 0
            fi

            git add data/ index.html
            git commit -m "chore: ensure Mahaska Iowa tax lien market"
            if git push origin HEAD:main; then
              exit 0
            fi

            echo "Publish raced with a newer main commit; regenerating from latest main (${attempt}/3)."
          done

          echo "Unable to publish Mahaska market output after 3 fresh-generation retries."
          exit 1
